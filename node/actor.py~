class Actor:
    """Actor is an abstract superclass defining the function call interface to
    be used by the main program loop"""
    def act(actionString,params):
        """actionString defines some action to be performed by the underlying
        concrete child class. params are to be passed to the selected function."""
        raise NotImplemented
